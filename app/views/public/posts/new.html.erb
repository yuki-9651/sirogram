<h1>投稿フォーム</h1>
<%= form_with model: @post do |f| %>

  <h4>城名</h4>
  <%= f.text_field :castle_name %>
  <h4>画像</h4>
  <%= f.file_field :image, accept: "image/*" %>

  <h4>投稿本文</h4>
  <%= f.text_area :body %>
  <%= f.submit '投稿' %>
<% end %>